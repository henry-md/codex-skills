interface:
  display_name: "Doctor"
  short_description: "Review a skill before rewriting it"
  default_prompt: "Use $doctor with an existing skill name to assess whether it needs structural changes before editing it."
policy:
  allow_implicit_invocation: false
