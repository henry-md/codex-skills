interface:
  display_name: "Doctor"
  short_description: "Suggest high-leverage codebase refactors"
  default_prompt: "Use $doctor to review this codebase and suggest the highest-leverage refactors, especially around oversized files, duplicated logic, and split sources of truth."
policy:
  allow_implicit_invocation: false
