interface:
  display_name: "Characterize Codex"
  short_description: "Publish safe ~/.codex/skills commits"
  default_prompt: "Use $characterize-codex to apply the $characterize push workflow to ~/.codex/skills, verify the commit stack, and push the skills repo once."
