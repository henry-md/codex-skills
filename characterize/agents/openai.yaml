interface:
  display_name: "Characterize"
  short_description: "Split repo changes into safe commit buckets"
  default_prompt: "Use $characterize to review the current repository changes and group them into safe commit buckets before staging or committing."
