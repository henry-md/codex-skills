interface:
  display_name: "Add"
  short_description: "Rebuild staged set for this chat"
  default_prompt: "Use $add to first clear any existing staged changes, then stage only the changes that belong to this conversation while leaving unrelated work unstaged. If the user invoked `/add push`, commit those staged changes with the generated message and push them."
policy:
  allow_implicit_invocation: false
