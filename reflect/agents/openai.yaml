interface:
  display_name: "Reflect"
  short_description: "Recommend cleanup and regression checks"
  default_prompt: "Use $reflect to review recent complex work, recommend only objectively worthwhile refactors, and include the regression checks needed to verify them."
