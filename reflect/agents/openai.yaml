interface:
  display_name: "Reflect"
  short_description: "Decide if recent work deserves cleanup"
  default_prompt: "Use $reflect to review recent complex work and decide whether a focused refactor is objectively worthwhile."
